
### Docker
- start docker
- docker build .
- docker-compose up

### Noraml

- install ruby (use rvm)
- install rails
- cd to project directory 
- bundle install
- rails db:create db:migrate
- rails s (to start the app)


